# Results

The committed figures and result tables are development/reproduction
outputs generated from the supplied local copy containing 16,383 features.

The official UCI dataset contains 20,531 features. Regenerate final
headline results using:

```bash
python -m src.run_analysis --source uci
```

The co-expression network is correlation-based. It is not evidence of
direct molecular interaction or causality.
